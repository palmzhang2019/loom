# ROADMAP.md

> 开发大节点(规划意图,**非实时进度**)。每个大节点挂 REQ-ID,这是 Loom ID 主线的起点。
> 进度不在此记录;此文件只表达"打算做什么、切成哪几刀"。

---

## MAT-REQ-001 · 素材详情页支持移除来源标签

> 用户能在素材详情页逐个移除误加的来源标签(硬删关联、二次确认、整页刷新)。
> 详见 `product.md`(what/why)与 `tech.md`(how)。

| Segment | 大节点 |
|---|---|
| MAT-REQ-001 / S1 | 后端:补按 `material_id + tag_id` 删除关联的 POST 路由 + `tagging.py` 的 remove 逻辑 |
| MAT-REQ-001 / S2 | 前端:素材详情页每个已挂标签旁加"移除"入口,带二次确认,提交到 S1 路由 |
| MAT-REQ-001 / S3 | 收尾:删除后重定向回素材详情页,给"标签已移除"反馈 |

---

> 后续需求在此向下追加,REQ-ID 全局连续编号(下一条为 `<模块>-REQ-002`)。
